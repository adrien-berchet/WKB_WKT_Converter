name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build source distribution
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          maturin-version: v1.11.5
          args: --out dist

      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*
          if-no-files-found: error

  build-linux-wheels:
    name: Build Linux wheel (${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Build manylinux wheel
        uses: PyO3/maturin-action@v1
        with:
          maturin-version: v1.11.5
          manylinux: "2014"
          args: >-
            --release
            --locked
            --strip
            --out dist
            --compatibility pypi
            -i python${{ matrix.python-version }}

      - name: Smoke test wheel
        shell: bash
        run: |
          python -m pip install --force-reinstall dist/*.whl
          python - <<'PY'
          import wkb_wkt_converter

          wkb = wkb_wkt_converter.wkt_to_wkb("POINT (1 2)")
          assert wkb_wkt_converter.wkb_to_wkt(wkb) == "POINT (1 2)"
          PY

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-x86_64-py${{ matrix.python-version }}
          path: dist/*
          if-no-files-found: error

  build-x64-native-wheels:
    name: Build ${{ matrix.platform.name }} wheel (${{ matrix.python-version }})
    runs-on: ${{ matrix.platform.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        platform:
          - os: macos-15-intel
            name: macos-x86_64
          - os: windows-latest
            name: windows-x86_64
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          maturin-version: v1.11.5
          args: >-
            --release
            --locked
            --strip
            --out dist
            --compatibility pypi
            -i python

      - name: Smoke test wheel
        shell: bash
        run: |
          python -m pip install --force-reinstall dist/*.whl
          python - <<'PY'
          import wkb_wkt_converter

          wkb = wkb_wkt_converter.wkt_to_wkb("POINT (1 2)")
          assert wkb_wkt_converter.wkb_to_wkt(wkb) == "POINT (1 2)"
          PY

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.platform.name }}-py${{ matrix.python-version }}
          path: dist/*
          if-no-files-found: error

  build-macos-arm64-wheels:
    name: Build macos-arm64 wheel (${{ matrix.python-version }})
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          maturin-version: v1.11.5
          args: >-
            --release
            --locked
            --strip
            --out dist
            --compatibility pypi
            -i python

      - name: Smoke test wheel
        shell: bash
        run: |
          python -m pip install --force-reinstall dist/*.whl
          python - <<'PY'
          import wkb_wkt_converter

          wkb = wkb_wkt_converter.wkt_to_wkb("POINT (1 2)")
          assert wkb_wkt_converter.wkb_to_wkt(wkb) == "POINT (1 2)"
          PY

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-arm64-py${{ matrix.python-version }}
          path: dist/*
          if-no-files-found: error

  publish-pypi:
    name: Publish to PyPI
    if: startsWith(github.ref, 'refs/tags/v')
    needs:
      - build-sdist
      - build-linux-wheels
      - build-x64-native-wheels
      - build-macos-arm64-wheels
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/wkb-wkt-converter/
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: List distributions
        run: ls -l dist

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
